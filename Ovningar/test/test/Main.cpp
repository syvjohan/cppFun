#include <string.h>
#include <vector>
#include <iostream>

using namespace std;

int main()
{
	cout<<"hje"<<endl;
	/*vector<int> v1;
	vector<int> v2;

	for ( int i = 0; i < v1.size() || i == 10; i++)
	{
		v1.push_back(i);
		for (auto &m : v1)
		{
			cout<<i<<endl;
		}
	}

	for ( int k = 0; k < v2.size() || k == 10; k++)
	{
		v1.push_back(k);
		for (auto &n : v2)
		{
			cout<<k<<endl;
		}
	}*/

	system("PAUSE");
}
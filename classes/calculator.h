#ifndef CALCULATOR_H
#define CALCULATOR_H

template <class T> class Calculator {
public:

	T Multiplication(T a, T b);
	T Division(T a, T b);
	T Addition(T a, T b);
	T Substraction(T a, T b);

private:
};

#endif // !CALCULATOR_H